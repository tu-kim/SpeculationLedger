# [G1] 재발률: 검증 산출물이 재발하는가 — proposer 없이 trace 자체의 재발 구조 측정.
# 현재 trace는 합성(synthetic)이므로 이 판정은 하네스 검증용이다 (DECISIONS A-3).
# 실제 trace 확보 시 trace.paths만 교체한다.
exp_id: g1_recurrence
gates: [G1]
seed: 20260711
budget: 8
trace:
  provenance: synthetic
  paths: ["traces/synth/default.jsonl"]
  synth:
    seed: 20260711
    repos: 2
    sessions_per_repo: 4
    turns_per_session: 8
proposers: []
thresholds:
  assumed: true
  g1_min_rate_order4_session: 0.30
